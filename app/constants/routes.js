const APP_PREFIX = '/app';
const SETTING_PREFIX = '/setting';

const ROOT_ROUTES = {
  HOMEPAGE: '/',
  FEATURES: '/features'
};

const APP_ROUTES = {
  NOTE_CASE: `${APP_PREFIX}/nc`
};

export {
  ROOT_ROUTES,
  APP_ROUTES
};